const Account = () => {
  return (
    <div className="Account">
      <form action="">
        <label htmlFor="">Recipient</label>
        <input type="text" />

        <label htmlFor="">Amount</label>
        <input type="text" />
        <button>Send</button>
      </form>
    </div>
  );
};

export default Account;
